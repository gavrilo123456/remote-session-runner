# P062 implementation evidence

## Pre-phase checkpoint

- Phase: `P062`
- Scope: Run the daemon-level Mac account and source-provenance suites through `runner-locald` for the selected `tomasz.walczuk` account. This is the first real Mac `P-MAC-01`/`P-MAC-02` gate; it must not be inferred from fake runtime tests or direct adapter-only tests.
- Mac checkout: `/Users/tomasz.walczuk/projects/remote-session-runner`
- Pre-phase HEAD: `4c6081234a7b12b95e0ce98c202ba7f197b50994`
- Branch/worktree: `dev`, clean before this P062 evidence file
- P061 final synchronization: Mac, GitHub `dev`, and Ubuntu were clean and equal at `4c6081234a7b12b95e0ce98c202ba7f197b50994`.

## Fresh-context reads

The complete current root `AGENTS.md`, `README.md`, initial design source `010-initial-idea/020-md/runner_remote_execution_detailed_design_v2.md`, detailed phased plan `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md`, pre-implementation decisions `040-implementation-evidence/000-preimplementation-decisions.md`, and committed prior evidence `P061.md` were read through end-of-file before coding. Current Mac process adapter, persistent shell, locald runtime/server/operation code, domain environment/source policies, SQLite intent/authority code, and earlier P037/P038 real-adapter tests were inspected completely.

Current P062 pre-phase SHA-256 values:

| Document | SHA-256 |
| --- | --- |
| `AGENTS.md` | `e36fa95e449e1ee1f41d66376f3be31ddfa6a1d18435f74e0ba8d8e8235e6e2c` |
| `README.md` | `18dc56140b66dbd5274f0fdbcbe918afe61064e5d554180ae83553b2a4ad98fa` |
| `010-initial-idea/020-md/runner_remote_execution_detailed_design_v2.md` | `24c59a578d2731df04233aba7e0bf15d84ce434420845b2a76391fcdadce7327` |
| `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md` | `cebaf6eedbdc61e2335ec472c95fb22c2b283d432ee11f31b54b62ba1f1c1de5` |
| `040-implementation-evidence/000-preimplementation-decisions.md` | `968645d5f7bdb6220e39b6800c00ed39106dfe26283c1f631edd0b2ded5142b2` |
| `040-implementation-evidence/P061.md` | `6080f29e916f41547a93b9c37f48b34136128a99c852fa34825acad9c29833a6` |

## Planned deliverable and real-host gate

- Exercise the shared execution service and `MacSessionRuntime` through the owner-only locald socket, not by calling the adapter directly.
- Verify the actual Mac process identity (`tomasz.walczuk`, UID 501), Bash child identity, owner-only workspace and session lifecycle for `empty` source.
- Verify `git_revision` resolves the exact current repository commit through an explicit `runner` alias and starts in a detached materialized workspace with the same commit.
- Verify `local_worktree` uses the selected absolute worktree as a starting directory, exposes its uncommitted marker, remains non-portable, and is not claimed as a filesystem boundary.
- Close each real session through locald and assert target-authority terminal state and cleanup. Record the machine, OS, account, UID, paths, and observed outputs.

P063 remains the next remote queued Router phase. P143 remains a future approval gate: if physical power-cut testing is unavailable, a software-crash-only durability claim requires explicit user approval and must not be inferred from P062.

## Real Mac gate result

- Added `src/internal/runnerlocald/p062_test.go`, a daemon-level fixture that constructs the shared Mac execution service and calls it only through the owner-only locald socket. It covers empty, exact `git_revision`, and explicit `local_worktree` sources, then closes every session through locald.
- Mac machine evidence: `hostname` = `AMAK2KJ6X9JJJ`; `uname -s` = `Darwin`; `id -un` = `tomasz.walczuk`; `id -u` = `501`; Go = `go1.27.1 darwin/arm64`.
- Focused Mac command: `GOTOOLCHAIN=local /Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go test ./src/internal/runnerlocald -run '^TestP06[012]' -count=1 -v` — PASS. Observed output included `tomasz.walczuk|501|.../workspaces/session-*` for the empty source, workspace mode `0700`, `visible-from-local-worktree` from the uncommitted marker, and exact Git revision `4c6081234a7b12b95e0ce98c202ba7f197b50994` from the detached Git source.
- The local-worktree directory and its marker remained after session cleanup; owned empty/git workspaces were removed. This confirms the chosen worktree is a starting directory and is not treated as a filesystem boundary.
- Mac repository gates: `make test` — PASS; `make vet` — PASS; `make build` — PASS; `make smoke` — PASS; `GOTOOLCHAIN=local /Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go test -race ./...` — PASS; `git diff --check` — PASS.

## GitHub and checkout synchronization

- Implementation/test commit: `8ed659ad3f2b922db858c833985c7da48fa222b1` (`phase(P062): verify Mac locald provenance`).
- Mac pushed `dev` with `/Users/tomasz.walczuk/.ssh/gavrilo123456-github`; GitHub `refs/heads/dev` resolved to the same full commit.
- Ubuntu `/home/ubuntu/projects/remote-session-runner` was verified on clean `dev` at `4c6081234a7b12b95e0ce98c202ba7f197b50994`, then fast-forward pulled `origin/dev` using `/home/ubuntu/.ssh/gavrilo123456-github`.
- Before advancing, Mac, GitHub `dev`, and Ubuntu all resolved to `8ed659ad3f2b922db858c833985c7da48fa222b1`; Ubuntu was clean. The required P062 host gate is the named real Mac gate; no Ubuntu result is treated as a P062 Mac pass.

## Phase result

**P062 PASS.** Through `runner-locald`, the selected Mac account `tomasz.walczuk` (UID 501) executed local sessions with owner-only workspaces, exact Git provenance, and explicit local-worktree behavior. The real Mac `P-MAC-01` and `P-MAC-02` gate passed, with no filesystem-confinement claim. P063 is the next queued Router phase. P143 software-crash-only durability remains an explicit approval gate if physical power-cut tests are unavailable.
