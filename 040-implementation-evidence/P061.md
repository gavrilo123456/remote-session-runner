# P061 implementation evidence

## Pre-phase checkpoint

- Phase: `P061`
- Scope: Add `runner-locald` private read, cancel, close, and command-event subscription calls by stable resource ID. The locald server remains owner-only and calls the shared `execution.Service`; it does not accept arbitrary mutation payloads or create a second execution core.
- Mac checkout: `/Users/tomasz.walczuk/projects/remote-session-runner`
- Pre-phase HEAD: `197be36719f1910e51c20bd31ea99c1c76ec57c7`
- Branch/worktree: `dev`, clean before this P061 evidence file
- P060 final synchronization: Mac, GitHub `dev`, and Ubuntu were clean and equal at `197be36719f1910e51c20bd31ea99c1c76ec57c7`.

## Fresh-context reads

The complete current root `AGENTS.md`, `README.md`, initial design source `010-initial-idea/020-md/runner_remote_execution_detailed_design_v2.md`, detailed phased plan `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md`, pre-implementation decisions `040-implementation-evidence/000-preimplementation-decisions.md`, and committed prior evidence `P060.md` were read through end-of-file before coding. Current `runnerlocald`, `runnerd` private API, shared execution service read/mutation/event methods, authority records, command subscriptions, domain controller/ID validation, and private API tests were inspected completely.

Current P061 pre-phase SHA-256 values:

| Document | SHA-256 |
| --- | --- |
| `AGENTS.md` | `e36fa95e449e1ee1f41d66376f3be31ddfa6a1d18435f74e0ba8d8e8235e6e2c` |
| `README.md` | `18dc56140b66dbd5274f0fdbcbe918afe61064e5d554180ae83553b2a4ad98fa` |
| `010-initial-idea/020-md/runner_remote_execution_detailed_design_v2.md` | `24c59a578d2731df04233aba7e0bf15d84ce434420845b2a76391fcdadce7327` |
| `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md` | `cebaf6eedbdc61e2335ec472c95fb22c2b283d432ee11f31b54b62ba1f1c1de5` |
| `040-implementation-evidence/000-preimplementation-decisions.md` | `968645d5f7bdb6220e39b6800c00ed39106dfe26283c1f631edd0b2ded5142b2` |
| `040-implementation-evidence/P060.md` | `75d0fa6ddca916ca5b68bb6c27852af8d92c1927bc2fb41ccca72b4b8afe6b09` |

## Planned deliverable

- Bind locald to one configured owner controller identity and derive that identity from the selected Mac account in the daemon wiring.
- Add stable-ID private `GET` session/command reads, `GET` command events with replay cursor and optional follow, `POST` command cancel, and `DELETE` session close routes.
- Use the shared service for all reads and mutations, preserve keyed idempotency/conflict behavior, and map controller/ID/state errors without exposing arbitrary script or controller payloads.
- Require owner-only access for every route, reject controller fields and unknown mutation fields, keep event output bounded and base64 encoded, and close subscriptions on terminal event, disconnect, or overflow.
- Add focused private-operation, owner/controller, stable-ID, event replay/follow, cancel/close idempotency, and forged-payload tests. This phase does not claim the real Mac account/source gate (P062), remote HTTPS, SSH, or Linux execution.

P062 remains the real Mac account/source-provenance gate. P143 remains a future approval gate: if physical power-cut testing is unavailable, a software-crash-only durability claim requires explicit user approval and must not be inferred from P061.

## Implementation and Mac gates

- Bound `runner-locald` to one configured owner controller. The daemon derives `(local_user, settings.Account)` from the Mac configuration; test construction defaults only preserve the selected `tomasz.walczuk` PoC account.
- Added stable-ID private session/command reads, replay and follow event streams, keyed command cancellation, and keyed session closure. All routes call the shared `execution.Service`, enforce the fixed owner controller, reject caller controller query fields, reject unknown mutation fields, and reject non-local resources.
- Event responses use NDJSON with base64 stdout/stderr payloads, durable sequence cursors, terminal-event completion, request-disconnect cleanup, and bounded subscription capacity. Cancel and close preserve shared idempotency/conflict behavior; scripts and other arbitrary fields are never accepted by these private mutation routes.
- Focused Mac gate: `GOTOOLCHAIN=local /Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go test ./src/internal/runnerlocald -run '^TestP06[01]' -count=1` — PASS (host Unix-socket context). Fixtures cover stable reads, replay/follow, owner/controller denial, cancel/close, duplicate replay, changed close policy, stable IDs, and forged payloads.
- Mac repository gates: `make test` — PASS; `make vet` — PASS; `make build` — PASS; `make smoke` — PASS; `GOTOOLCHAIN=local /Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go test -race ./...` — PASS; `git diff --check` — PASS.

## GitHub and Ubuntu synchronization

- Implementation commit: `9c8e48685b33d1906beb83a3d739eace6d1131ed` (`phase(P061): add locald resource operations`).
- Mac pushed `dev` with `/Users/tomasz.walczuk/.ssh/gavrilo123456-github`; GitHub `refs/heads/dev` resolved to the same full commit.
- Ubuntu `/home/ubuntu/projects/remote-session-runner` was verified on clean `dev` at `197be36719f1910e51c20bd31ea99c1c76ec57c7`, then fast-forward pulled `origin/dev` using `/home/ubuntu/.ssh/gavrilo123456-github`.
- Before the host gate, Mac, GitHub `dev`, and Ubuntu all resolved to `9c8e48685b33d1906beb83a3d739eace6d1131ed`; Ubuntu was clean. The Mac worktree contained only this in-progress evidence file, intentionally excluded from the implementation commit.

## Named P061 host gate

- Ubuntu command: `GOTOOLCHAIN=local /home/ubuntu/.local/share/remote-session-runner/toolchains/go1.27.1/bin/go test ./src/internal/runnerlocald -run '^TestP061' -count=1` — PASS (`ok remote-session-runner/src/internal/runnerlocald 0.137s`).
- The host gate ran the synchronized private read, command read, replay/follow events, owner/controller denial, cancel/close idempotency, changed-policy conflict, stable-ID, and forged-payload fixtures. It is a locald/shared-service gate; it is not a Linux Runner application test, public HTTPS/mTLS test, or remote command execution claim.

## Phase result

**P061 PASS.** `runner-locald` now exposes owner-bound stable-ID reads, event replay/follow, cancel, and close operations through the shared execution service. Mac gates, GitHub delivery, checkout equality, and the required Ubuntu host subset passed. P062 owns the real Mac account/source-provenance gate. P143 software-crash-only durability remains an explicit approval gate if physical power-cut tests are unavailable.
