# P021 implementation evidence

**Status: PASS**

## Pre-phase scope and authority

- **Phase:** P021 — fake-runtime command start/output/terminal behavior and shell-exit versus transport distinction.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** all versioned changes are made on the Mac. Ubuntu receives only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `4da83fe65bf5d380abe3ef219edf6af33330e09e` (`docs(P020): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P020 implementation `f324675376edea2113e62dd15df5d005c447584e`; P020 closeout `4da83fe65bf5d380abe3ef219edf6af33330e09e`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P020.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including detailed §§5–7, 14.1 (`D-04`, `D-11`), 14.2 (`I-02`), 15–17, the P021 row, and the P143 limitation;
- current `src/internal/execution/service.go`, P020 fake-runtime tests, domain state/limits/policy/identity types, store commands/scheduler/subscriber/session code, SQLite migrations, package docs, and all existing store/execution tests.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P020 evidence | recorded in the prior closeout commit |

## Planned deliverable and gates

1. Add shared service command submission/read orchestration over the durable command store and scheduler; commands are accepted before any fake runtime call.
2. Add a fake command runtime result contract that emits stdout/stderr bytes, exits with a command code, or reports a transport/runtime failure distinctly.
3. Persist output events before terminal completion, transition the command atomically, return the session to `ready` on a clean shell boundary, and preserve the session/command distinction for shell exit versus transport failure.

Required gates: focused `D-04`/`D-11`/`I-02`/fake `I-01` tests, focused P011–P021 tests, full `make test`, `make vet`, selected-toolchain gofmt, `git diff --check`, race-enabled execution/store tests, and the Mac commit → GitHub push → clean Ubuntu fast-forward pull → equal-hash verification. No host-dependent runtime, SSH, HTTPS, or Runner application TLS test is assigned to P021. P143’s software-crash-only durability path still requires explicit user approval if physical power-cut tests are unavailable.

## Implementation and test results

- **Implementation commit (Mac):** `fe85d5be0661154cccc48c78724671ed807a19de` (`phase(P021): add fake command execution outcomes`).
- Added shared command submission and read orchestration, optional command-capable runtime seam, raw output persistence, and an atomic store completion transaction that appends the terminal event, updates command metadata, transitions the busy session, and releases or retains the durable command slot.
- **Focused P011–P021 gate (Mac, exit 0):** selected Go 1.27.1 `go test ./src/internal/execution ./src/internal/store -run '^TestP021|^TestP020|^TestP019|^TestP018|^TestP017|^TestP016|^TestP015|^TestP014|^TestP013|^TestP012|^TestP011' -count=1` — PASS.
- **P021 race gate (Mac, exit 0):** selected Go 1.27.1 `go test -race ./src/internal/execution ./src/internal/store -count=1` — PASS.
- **Full hermetic suite (Mac, exit 0):** `GOTOOLCHAIN=local make test` (`go test ./...`) — PASS.
- **Full race suite (Mac, exit 0):** selected Go 1.27.1 `go test -race ./... -count=1` — PASS.
- **Vet gate (Mac, exit 0):** `GOTOOLCHAIN=local make vet` (`go vet ./...`) — PASS.
- **Formatting/integrity (Mac, exit 0):** selected-toolchain `go fmt ./src/internal/execution ./src/internal/store`, followed by `git diff --check` — PASS.
- **Focused assertions:** output bytes are stored before `command_succeeded`/`command_failed`; a nonzero shell exit returns `failed` with its exit code and a ready session; a shell-exit boundary records `lost`/incomplete output and a lost session; transport failure is a distinct `ErrCommandTransport` and does not rerun the command; normal completion releases its slot while loss retains it; same-key submit returns the original terminal command without a second runtime call; pre-ready submission creates no command.
- The known Go 1.27.1 telemetry upload-token permission warning appeared in the Codex shell; all commands exited 0. No actual Mac/Linux process, SSH, public HTTPS, or Runner application TLS gate is claimed by P021.

## Git handoff and closeout

- **Mac pre-push:** implementation commit `fe85d5be0661154cccc48c78724671ed807a19de` on `dev`; only this evidence file remained untracked.
- **GitHub:** `origin/dev` verified at `fe85d5be0661154cccc48c78724671ed807a19de` using `/Users/tomasz.walczuk/.ssh/gavrilo123456-github`.
- **Ubuntu pre-pull:** `/home/ubuntu/projects/remote-session-runner` was clean on `dev` at `4da83fe65bf5d380abe3ef219edf6af33330e09e`.
- **Ubuntu pull:** fast-forward `4da83fe65bf5d380abe3ef219edf6af33330e09e` → `fe85d5be0661154cccc48c78724671ed807a19de` using `/home/ubuntu/.ssh/gavrilo123456-github`; Ubuntu `HEAD` and `origin/dev` were verified at the implementation hash and the worktree was clean.
- **Host-dependent gates:** NOT RUN; none are required for P021. The temporary TLS probe remains distinct from a Runner application test.
- **Next phase:** P022, only after this evidence closeout is committed, pushed, pulled, and both checkouts are verified equal and clean.
