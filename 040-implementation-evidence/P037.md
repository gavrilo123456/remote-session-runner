# P037 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P037 — add the macOS process adapter under the configured `tomasz.walczuk` account.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** versioned changes were made on the Mac. Ubuntu received only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `ae33421a93be8c38a075bfea5fa7eb0b5b22e835` (`docs(P036): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P036 implementation `190eca87ebf3f356883a92c098338be42cb20691`; P036 closeout `ae33421a93be8c38a075bfea5fa7eb0b5b22e835`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P036.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including P-MAC-01, the P037 row, configured `tomasz.walczuk` account rules, owner-only workspaces, process identity, explicit no-confinement claims, and the P143 limitation;
- current P030–P036 runtime implementation/tests, execution runtime seams, and the complete prior test gates.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P036 evidence | `a10460c52a7bf00d5b3862ae9a8e9df7396cc6e5` |

## Planned deliverable and gates

1. Verify the configured account is the current macOS user before constructing the adapter; do not use sudo or silently switch credentials.
2. Create a private 0700 per-session workspace, start one `PersistentShell` there, and expose shell/inspect/cleanup seams without claiming a path confinement boundary.
3. Preserve the P030–P036 control, output, barrier, stop, and retention behavior through the adapter.
4. Exercise the actual Mac account, mode-0700 workspace, real Bash command, identity/work-directory output, and cleanup.

Required gates: focused P037 Mac test, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. This phase does not claim the full P-MAC-01 worktree-permission matrix; source/provenance is P038. No host-dependent SSH, HTTPS, mTLS, or Runner application test is assigned to P037. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Implementation results

**Status: PASS**

- Added `src/internal/runtime/mac.go` with `MacProcessAdapter`, account validation against `user.Current`, owner-only workspace preparation, persistent Bash startup, shell access, process inspection, and cleanup.
- The adapter uses the current configured account directly and records that a worktree is only a starting directory; it does not claim filesystem confinement or add credential isolation.
- Added `src/internal/runtime/p037_test.go`, which ran on this macOS host as `tomasz.walczuk`, validated mode-0700 workspace creation, started a real Bash, ran identity/work-directory output, and removed the session workspace.

The implementation commit was `d338b0cd8bfbedcc8b695c678f6a953e539612c8` (`phase(P037): add mac process adapter`).

### Mac gates

All commands ran in `/Users/tomasz.walczuk/projects/remote-session-runner` as `tomasz.walczuk` with Go 1.27.1 and hermetic `GOCACHE`, `GOMODCACHE`, and `GOPATH` under `/private/tmp`:

```text
go test ./src/internal/runtime -run '^TestP03[01234567]' -count=1 PASS
go test ./src/internal/store ./src/internal/execution -count=1    PASS
make test                                                          PASS
make vet                                                           PASS
make smoke                                                         PASS
go test -race ./...                                                PASS
find src -name '*.go' -print0 | xargs -0 gofmt -d               PASS (no output)
git diff --check                                                     PASS
```

The selected Go binary emitted the known non-fatal telemetry warning because the Codex shell cannot write the user's Go telemetry upload-token path. It did not affect any test, vet, build, smoke, or race result.

No host-dependent SSH, HTTPS, mTLS, runtime, or Runner application test was assigned to P037. The temporary TLS probe remains transport evidence only, and P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Git handoff and closeout

- Mac implementation commit: `d338b0cd8bfbedcc8b695c678f6a953e539612c8`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu `/home/ubuntu/projects/remote-session-runner`: was clean on `dev` at `ae33421a93be8c38a075bfea5fa7eb0b5b22e835`, then fast-forward pulled `origin/dev` with the specified Ubuntu key to `d338b0cd8bfbedcc8b695c678f6a953e539612c8`.
- This evidence file is the separate P037 closeout change; it must be pushed and fast-forwarded before the next phase begins.
