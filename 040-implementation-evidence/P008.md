# P008 — pure session and command transition rules

## Pre-phase record

- **Status:** PASS for the P008 deliverable and Mac-local automated gates; the phase commit and GitHub/Ubuntu handoff are pending.
- **Pre-phase HEAD:** `36f8ba0892b3ff6872fd063a9e1cdb27b086349e` on `dev`.
- **Previous phase:** P007 implementation commit `64d1af528653c478a7839054c70d29efb3b0cdc7`; delivery closeout/current baseline `36f8ba0892b3ff6872fd063a9e1cdb27b086349e`.
- **Baseline delivery check:** Mac is clean on `dev` at `36f8ba0892b3ff6872fd063a9e1cdb27b086349e`; GitHub `refs/heads/dev` reports that hash using the specified Mac key; Ubuntu `/home/ubuntu/projects/remote-session-runner` is clean on `dev` with `HEAD` and `origin/dev` at that same hash.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, user `tomasz.walczuk`, Darwin 25.6.0 arm64. Verified toolchain: Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`. Planned temporary Go paths: `GOCACHE=/private/tmp/remote-session-runner-p008-go-cache`, `GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache`, `GOPATH=/private/tmp/remote-session-runner-p008-gopath`, `GOTOOLCHAIN=local`.
- **P002 evidence preservation:** `040-implementation-evidence/P002.md` was inspected and left untouched; its SHA-256 is `39e3fef7bb87589b47389743277bbbe712172ccde10220e4273f8ca02235a957` and its HEAD blob is `6f51b30e071cec86d49a8ee8c853f38cdde022bc`. It was tracked at the pre-phase baseline and matched HEAD; it is not currently an untracked file.

## Fresh-context read inventory

Read from the current Mac checkout before coding:

- Root `AGENTS.md` (28 lines), `README.md` (2 lines), `go.mod`, and `Makefile`; confirmed there are no nested `AGENTS.md` files.
- Full phased plan (267 lines), initial design (566 lines), and detailed design (605 lines), each reread from disk through EOF after context reset.
- Full pre-implementation decisions (74 lines), P007 evidence (64 lines), and preserved P002 evidence (63 lines).
- P007 implementation commit `64d1af528653c478a7839054c70d29efb3b0cdc7` and its complete scoped diff; P007 evidence closeout commit `36f8ba0892b3ff6872fd063a9e1cdb27b086349e` and its evidence-only diff.
- `src/internal/domain/doc.go`, `identity.go`, `identity_test.go`, `import_boundary_test.go`, and `schema_test.go`.
- P002 `src/internal/domain/schemas/v1/states.schema.json`, schema README, and `fixtures/valid/states--all-current.json`.
- Full `src` tracked-file inventory and searches for existing session/command state or transition definitions; none exist yet. Existing `ExecutionTarget` is a value type with private fields and accessors.
- Current Git status/HEAD, GitHub `dev`, and Ubuntu checkout status/HEAD were checked before this record was created.

Design/plan blob revisions at this baseline: plan `3f7de2b5ed99332636f15b9d947be5d5662e5f53`; initial design `12440598a0dba018313196cd80c7848284868f28`; detailed design `b24f095d62e6000abed690abf125fd60be4eff1c`; decisions `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec`; root instructions `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5`; README `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5`; P007 evidence `f7e598dcd1ffc58043065c6fecf3c595f8632ac0`; P002 evidence `6f51b30e071cec86d49a8ee8c853f38cdde022bc`.

## Scope and gate

Implement pure session/command state vocabularies, allowed-edge tables, and terminal-state checks for the D-01 subset. Keep the session execution target immutable across state transitions. A command belongs to a session and has no target override. This phase does not add persistence, event transactions, runtime conditions, or host adapters; those remain with their later phases, including P017's full D-01 gate.

Planned checks from the repository root on this Mac:

1. Focused transition/terminal/target tests: `go test ./src/internal/domain -run '^TestP008D01' -count=1` with the recorded Go 1.27.1 toolchain and temporary cache paths.
2. Full currently available hermetic suite: `make test` with `GOTOOLCHAIN=local` and the same temporary paths. This includes the D-12 import-boundary test.
3. `make vet` with the same toolchain and temporary paths.
4. `gofmt` on changed Go files, `git diff --check`, and a full diff review.
5. No race test is assigned because this phase adds no concurrent behavior. P008 has no named real-process or host test fixture; real host tests are **NOT RUN** and are not an exit gate for this pure domain phase. No Runner application test is claimed from the temporary TLS probe.

On passing gates, finalize this evidence, create one non-empty `phase(P008): ...` commit on Mac, push `dev` with `/Users/tomasz.walczuk/.ssh/gavrilo123456-github`, then fast-forward Ubuntu with `/home/ubuntu/.ssh/gavrilo123456-github`. Verify Mac, GitHub, and Ubuntu all have the same commit before starting P009.

## Prerequisites and unresolved assumptions

- P007 is delivered through its implementation and evidence-closeout commits. The current baseline is clean and synchronized across Mac, GitHub `dev`, and Ubuntu.
- P002 already defines the state vocabulary in the checked-in schema. P008 must use those exact values and transitions from detailed design §3.1; `requested` and `not_delivered` remain distinct because `not_delivered` is a delivery outcome, not an authoritative session state.
- D-01's state/event atomicity, transition-specific runtime conditions, and persistence behavior are outside this pure subset and remain unproven until their owning phases.
- Physical power-cut tests are not available on the current Linux host. P143's software-crash-only path still requires explicit user approval if physical tests remain unavailable; no approval is inferred.

## Implementation and local verification

Added `SessionState` and `CommandState` vocabularies with exhaustive D-01 edge tables, validity checks, transition validation, and terminal-state predicates. Added immutable `SessionLifecycle` and `CommandLifecycle` values: session transitions preserve ID and target, while commands retain only their parent session ID and expose no target override. The state constants are checked against the P002 v1 JSON Schema. These are pure domain rules; no database transaction, lifecycle/event append, runtime condition, or host behavior is implemented here. Full D-01 remains owned by P017.

On the Mac (`tomasz.walczuk`, Darwin 25.6.0 arm64, Go 1.27.1), with `GOTOOLCHAIN=local`, `GOCACHE=/private/tmp/remote-session-runner-p008-go-cache`, `GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache`, and `GOPATH=/private/tmp/remote-session-runner-p008-gopath`:

1. `"/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" test ./src/internal/domain -run '^TestP008D01' -count=1` — PASS, exit 0 (`ok remote-session-runner/src/internal/domain 0.550s`).
2. `make test` — PASS, exit 0 for all packages; this includes the D-12 import-boundary test.
3. `make vet` — PASS, exit 0.
4. `gofmt -w src/internal/domain/state.go src/internal/domain/state_test.go`, followed by `gofmt -d` on those files — PASS, no formatting diff.
5. `git diff --cached --check` — PASS, exit 0 after staging only the three P008 files; the complete staged phase diff was reviewed.

No race test is required for this non-concurrent change. P008 defines no real-process or host test, so the real-host suite is NOT RUN and is not an exit gate. No Runner application test or TLS application result is claimed.
