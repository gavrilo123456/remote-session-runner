# P006 — environment, source, limit, and capability policy types

## Pre-phase record

- **Status:** PASS for implementation and local hermetic gates; the phase commit and GitHub/Ubuntu handoff are pending.
- **Pre-phase HEAD:** `b2c7311d0d70fb485925abace95bebc90ab78dae` on `dev`.
- **P005 implementation and evidence commits:** `c9b72db54e0ce8f2a08c54ef75a78b99e92c62e0` and `b2c7311d0d70fb485925abace95bebc90ab78dae`.
- **Baseline delivery gate:** Mac HEAD, GitHub `refs/heads/dev`, and Ubuntu `/home/ubuntu/projects/remote-session-runner` all report `b2c7311d0d70fb485925abace95bebc90ab78dae`. Both worktrees are clean on `dev`. GitHub was queried with the specified Mac key. Ubuntu was checked over the configured strict-host-checked SSH alias; its known-hosts temporary-file warning did not prevent the read-only check.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, user `tomasz.walczuk` (UID 501), Darwin arm64. Go 1.27.1: `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`; use `GOTOOLCHAIN=local` and phase-specific caches under `/private/tmp`.

## Fresh-context read inventory

Read through EOF for the original P006 start, then repeated the full design reload after context compaction as required by plan §1.1, before finalizing the implementation and test record:

- `AGENTS.md`, `README.md`, and root `go.mod`
- `020-initial-design/010-remote-session-runner-initial-design.md` (566 lines)
- `030-detailed-design/010-remote-session-runner-detailed-design.md` (605 lines; §§3, 6.1, 10, 13, and D-02 reviewed for this phase)
- `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md` (267 lines; §1 protocol and P006 row reviewed)
- `040-implementation-evidence/000-preimplementation-decisions.md` (74 lines)
- `040-implementation-evidence/P005.md` and the P005 implementation/evidence commit summaries
- Opened and preserved `040-implementation-evidence/P002.md` unchanged at baseline blob `6f51b30e071cec86d49a8ee8c853f38cdde022bc`.
- Current P005 domain package sources/tests: `src/internal/domain/doc.go`, `identity.go`, `identity_test.go`, `import_boundary_test.go`, and `schema_test.go`; current P006 `policy.go` and `policy_test.go`
- P002 schema README plus `execution-target.schema.json`, `source.schema.json`, `session.schema.json`, and `command.schema.json`
- P003 OpenAPI component names and full `CreateSessionRequest`, `ExecutionTarget`, `Source`, and `SessionResource` definitions; full valid and invalid P003 contract-case fixtures
- `040-implementation-evidence/P005.md`, the P005 implementation commit diff (`c9b72db...`), and P005 evidence closeout diff (`b2c7311...`) were reviewed as the preceding phase evidence and diff.
- A repository search found no current configuration, migration, or deployment files relevant to this pure domain phase.

Baseline blobs: `AGENTS.md` `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5`, README `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5`, initial design `12440598a0dba018313196cd80c7848284868f28`, detailed design `b24f095d62e6000abed690abf125fd60be4eff1c`, phased plan `3f7de2b5ed99332636f15b9d947be5d5662e5f53`, decisions `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec`, P002 evidence `6f51b30e071cec86d49a8ee8c853f38cdde022bc`, P005 evidence `284622f557293709fca44871d3807743cd7c76e9`, and `go.mod` `c9dbe1d7c9e011c05e96349b21b350a6d2733c56`.

## P006 scope and gate

Add pure domain types and validation for environment definitions, compatible target kind/profile pairs, source modes/repository aliases, requested per-session service limits, unsupported host-isolation requirements, and truthful effective capabilities. Use constructed Mac/Linux environment fixtures from the selected pre-implementation decisions; P006 does not add the runtime configuration registry, which belongs to P010.

The focused gate is the P006 D-02 subset: compatible requests pass; target/profile, source, controller, and service-limit mismatches reject; requested per-session filesystem/CPU/memory/PID/disk/network/mount/volume/privilege controls reject on both host-process profiles; and capabilities report only the `os-user` boundary and selected service limits. Include tests for the selected `mac-dev` and `linux-dev` shapes and a named test-only repository alias. Run focused domain policy/import-boundary tests, then `go test ./...`, `go vet ./...`, formatting and diff checks. P006 adds no concurrent behavior. No real-process, host, SSH, HTTPS, or Runner application test is assigned; the earlier TLS probe remains transport-only evidence.

## Assumptions

- An omitted/zero requested session limit inherits the environment's selected limit; a lower request is enforced as requested, while a value above the service ceiling is rejected rather than silently clamped.
- `local_worktree` validation checks absolute-path shape only. Existence, canonical path, and account access belong to the later real Mac runtime gate. `git_revision` requires a non-empty configured repository alias and revision; resolving it to an exact commit belongs to later source-preparation phases.
- The kickoff has no production repository aliases. Tests may use an explicit fixture alias; arbitrary URLs are not treated as aliases.
- Environment and capability types are policy data only. P010 owns loading the actual per-host environment registry; later runtime phases must prove effective host behavior.
- P143's software-crash-only durability path still requires explicit user approval if representative physical power-cut tests are unavailable. No approval is assumed, and P006 passes no future durability or host gate.

## Implementation and delivery record

Added immutable source values and environment policies with exact target/profile, source-mode, repository-alias, and controller allowlists. The selected test fixtures are named `mac-dev` and `linux-dev`: Mac allows `empty` and absolute local-only `local_worktree` for `tomasz.walczuk`; Linux allows `empty` for `ubuntu`; Linux allows the distinct queued-Mac and direct-mTLS controllers. No production environment registry or repository alias was added; the one Git alias is an explicit test fixture only.

Added selected `ServiceLimits` and per-session reductions for command timeout, idle timeout, maximum session lifetime, and command output. Zero inherits the configured value; reductions and exact-ceiling requests pass; negative or over-ceiling values reject. Capability values expose the host class, actual account, `os-user`, and Runner service ceilings only. Filesystem, CPU, memory, PID, disk, network, mount, volume, and privilege requirements reject for both host-process policy fixtures. `local_worktree` checks absolute-path shape only and is never treated as filesystem confinement; runtime existence/access checks remain future work.

After the full context reload, the current source and focused domain assertions passed on the Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, user `tomasz.walczuk`, Darwin arm64, Go 1.27.1. Commands used `GOTOOLCHAIN=local`, `GOCACHE=/private/tmp/remote-session-runner-p006-go-cache`, `GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache`, and `GOPATH=/private/tmp/remote-session-runner-p006-gopath`:

1. `GOTOOLCHAIN=local GOCACHE=/private/tmp/remote-session-runner-p006-go-cache GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache GOPATH=/private/tmp/remote-session-runner-p006-gopath "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" test ./src/internal/domain -run '^TestP006' -count=1` — PASS, exit 0.
2. `GOTOOLCHAIN=local GOCACHE=/private/tmp/remote-session-runner-p006-go-cache GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache GOPATH=/private/tmp/remote-session-runner-p006-gopath "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" test ./...` — PASS, exit 0. This includes the current `TestP005ImportBoundaries` D-12 graph check.
3. `GOTOOLCHAIN=local GOCACHE=/private/tmp/remote-session-runner-p006-go-cache GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache GOPATH=/private/tmp/remote-session-runner-p006-gopath "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" vet ./...` — PASS, exit 0.
4. `"/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/gofmt" -d src/internal/domain/policy.go src/internal/domain/policy_test.go` — PASS, no output.
5. `git diff --check`, staged `git diff --cached --check`, and complete review of the staged evidence, implementation, and focused tests — PASS, exit 0.

P006 adds no concurrent behavior, so no race test is required. The plan assigns P006 a pure D-02 subset; the full fake-authority D-02 gate belongs to P020. No Runner application, real-process, Mac runtime, Linux runtime, SSH, HTTPS, or two-host suite is assigned or claimed in P006. The temporary TLS probe remains transport-only evidence. No future host gate is marked passed.

P143's software-crash-only durability path still requires explicit user approval if representative physical power-cut tests are unavailable. No approval is assumed. Do not begin P007 until the P006 phase commit and closeout evidence have been delivered and Mac, GitHub `dev`, and Ubuntu all match.
