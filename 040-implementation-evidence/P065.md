# P065 implementation evidence — Mac Unix API event replay/follow

## Pre-phase record

- Phase: `P065`
- Pre-phase HEAD: `05f1fa0144748e000614398f2249ac9da46d971b`
- Worktree: clean at the start of the phase; Mac, GitHub `dev`, and Ubuntu were synchronized at this commit.
- Authoritative source checkout: `/Users/tomasz.walczuk/projects/remote-session-runner` (Mac).
- Target execution machine for this phase: Mac local authority/API; no remote host execution is claimed.
- Required scope: Mac owner-only Unix API event replay/follow through the local SQLite authority store, with no handler-side SSH connection. Remote queued commands remain without local authority events until the later remote event mirror phases.
- P143 reminder: physical power-cut evidence and the software-crash-only durability path still require the user's explicit approval if physical tests are unavailable.

## Fresh-context read inventory

Read through end-of-file before implementation:

- `AGENTS.md`
- `README.md`
- `010-initial-idea/020-md/runner_remote_execution_detailed_design_v2.md`
- `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md`
- `040-implementation-evidence/000-preimplementation-decisions.md`
- `040-implementation-evidence/P064.md`
- `src/internal/localapi/server.go`
- `src/internal/localapi/p063_test.go`
- `src/internal/localapi/p064_test.go`
- `src/internal/store/local_intents.go`
- `src/internal/store/commands.go`
- `src/internal/store/sessions.go`
- `src/internal/store/subscribers.go`
- `src/internal/runnerlocald/server.go` (private event-stream reference)
- `src/internal/domain/schemas/v1/command-event.schema.json`

Relevant design/plan facts confirmed from those files:

- P065 is the Mac-local `I-03` subset and must not add a remote transport dependency to the Unix handler.
- The store already provides contiguous replay and register-before-replay bounded subscriptions.
- Public event JSON uses `timestamp`; output events use base64, `encoding`, and exact `byte_count`; lifecycle events carry no output fields.
- The local API accepts remote commands as local intents, but remote authority events are deferred to P074 and later.

## Planned gate

- Focused `src/internal/localapi` P065 replay/follow, cursor, output-byte, target-boundary, and no-SSH-import tests.
- Full `go test ./...`, `go vet ./...`, build/smoke/race gates, and `git diff --check` using the pinned Mac Go 1.27.1 toolchain.
- Inspect the phase-scoped diff, commit, push Mac `dev`, pull Ubuntu `dev` with `--ff-only`, and verify exact Mac/GitHub/Ubuntu commit equality before the next phase.

## Implementation and gate results

Implemented in the Mac checkout:

- `src/internal/localapi/server.go`: owner-scoped `/v1/commands/{id}/events`, contiguous replay, bounded live follow, cursor/follow validation, local-authority target/controller/hash/script checks, public `timestamp` NDJSON shape, queued ordinal, and base64 output chunks. The handler uses only the local authority store and imports no SSH package.
- `src/internal/localapi/p065_test.go`: replay and resume cursors, public schema fields, exact base64 output bytes, live replay/follow handoff to a terminal event, remote-intent boundary, and no-SSH behavior through the existing import tripwire.

Mac commands and results:

| Gate | Result |
| --- | --- |
| `go test ./src/internal/localapi -run TestP065 -count=1` | PASS |
| `go test ./src/internal/localapi` | PASS |
| `make test` (`go test ./...`) | PASS |
| `make vet` | PASS |
| `make build` | PASS |
| `make smoke` | PASS |
| `go test -race ./src/internal/localapi ./src/internal/store` | PASS |
| `git diff --check` | PASS |

No remote Runner application test or TLS probe is claimed by this Mac-local phase. P065 has no required Ubuntu runtime test; Ubuntu was used only for the mandated Git delivery synchronization.

## Delivery and synchronization

- Mac implementation commit: `f07ec4c6b9b0bee8504cf92524a0689bd7489430` (`phase(P065): add local API event replay and follow`).
- Mac push: explicit `/Users/tomasz.walczuk/.ssh/gavrilo123456-github` to `origin/dev`; PASS.
- GitHub `origin/dev`: `f07ec4c6b9b0bee8504cf92524a0689bd7489430`; PASS.
- Ubuntu pre-pull: branch `dev`, clean worktree, HEAD `05f1fa0144748e000614398f2249ac9da46d971b`.
- Ubuntu pull: explicit `/home/ubuntu/.ssh/gavrilo123456-github`, `pull --ff-only origin dev`; PASS.
- Ubuntu post-pull: HEAD `f07ec4c6b9b0bee8504cf92524a0689bd7489430`, clean worktree; PASS.
- Mac, GitHub `dev`, and Ubuntu therefore match exactly at the P065 implementation commit.

## Phase result

**PASS.** P065 is complete. Next phase: P066, after this evidence is committed, pushed, pulled fast-forward on Ubuntu, and the three revisions are reverified.

P143 remains unchanged: if physical power-cut tests are unavailable, the software-crash-only durability claim still requires explicit user approval; no future host gate is pre-passed by this evidence.
