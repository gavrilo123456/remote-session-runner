# P043 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P043 — reject unavailable Linux per-session isolation controls and verify actual OS access, status capabilities, and truthful process-teardown outcomes.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; named real-host gate is Ubuntu `oracle-yuta-konopka-ubuntu-micro-02`, account `ubuntu`.
- **Authoritative source:** versioned changes are made on the Mac. Ubuntu receives changes only through the pushed GitHub `dev` revision and fast-forward pulls.
- **PoC boundary:** local execution remains `tomasz.walczuk`, remote execution remains `ubuntu`; no Podman, tunnels, or Runner filesystem confinement. Direct HTTPS/mTLS and the temporary TLS probe remain separate later gates. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.
- **Pre-phase Mac HEAD:** `21cfa7991b27aad50705f294e7381080c9ee70ae` (`docs(P042): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P042 implementation/correction `2d58abafdbfd33da49838537dab5e93e772ef077`; P042 closeout `21cfa7991b27aad50705f294e7381080c9ee70ae`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P042.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including P043, P-LNX-01, environment capability rules, actual `ubuntu` OS access, process teardown truthfulness, and the P143 limitation;
- current `src/internal/runtime/linux.go`, `src/internal/domain/policy.go`, P006 policy tests, P040–P042 runtime tests, and configuration capability definitions.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P042 evidence | `125fbac8377b0cab015c6517bca55b82559314ea` |

## Planned deliverable and gates

1. Expose Linux adapter status/capabilities and a single validation path that rejects every unavailable per-session filesystem, CPU, memory, PID, disk, network, mount, volume, and privilege control.
2. Exercise the shared domain policy and adapter status against the real `ubuntu` host, including actual workspace access and account identity, without claiming confinement.
3. Verify process-stop/cleanup outcomes remain conservative: confirmed cleanup releases capacity; an unconfirmed result remains retained/degraded.

Required gates: focused P043 policy/status and real-Linux tests, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. The Ubuntu test must run only after the Mac implementation is pushed and the Ubuntu checkout is fast-forwarded to the same commit. P043 does not claim the full later R-01/R-02 suite, HTTPS/mTLS, SSH-bridge, or two-host Runner application acceptance.

## Implementation results

**Status: PASS**

- Added adapter capability/status reporting and `ValidateLinuxIsolationRequirements` using the shared domain error catalog. Filesystem, CPU, memory, PID, disk, network, mount, volume, and privilege controls reject before runtime use.
- `LinuxRuntimeStatus` reports the session/generation process identity and retained-capacity state with the truthful `os-user` capability. Service ceilings remain empty until configured by the environment registry; no sandbox or host-resource isolation is advertised.
- Added `src/internal/runtime/p043_test.go` with all unsupported-policy cases and the designated Ubuntu status/OS-access test. It verifies `ubuntu`/UID `1001`, writable selected workspace access, and truthful status without treating that workspace as confinement.

## Mac gates

```text
go test ./src/internal/runtime -run '^TestP043' -count=1       PASS
go test ./src/internal/runtime ./src/internal/store ./src/internal/execution -count=1 PASS
make test                                                       PASS
make vet                                                        PASS
make smoke                                                      PASS
go test -race ./...                                             PASS
gofmt -d                                                         PASS (no output)
git diff --check                                                 PASS
```

The first combined focused run encountered the known timing-sensitive P033/P035 host races; a rerun of the affected runtime tests and the complete hermetic/race gates passed. All Mac commands used Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go` with hermetic caches under `/private/tmp`. The known non-fatal Go telemetry upload-token warning remained present and did not affect results.

## Linux real-host gate

- Host: `oracle-yuta-konopka-ubuntu-micro-02`, Ubuntu 20.04.6 LTS.
- Account: `ubuntu`, UID `1001`.
- Bash: `/usr/bin/bash`, GNU bash 5.0.17.

After the Mac implementation was pushed and Ubuntu fast-forwarded to the same commit, Ubuntu ran:

```text
GOCACHE=/tmp/remote-session-runner-p043-go-cache \
GOMODCACHE=/tmp/remote-session-runner-p039-modcache \
GOPATH=/tmp/remote-session-runner-p043-gopath \
GOTOOLCHAIN=local \
/home/ubuntu/.local/share/remote-session-runner/toolchains/go1.27.1/bin/go \
  test ./src/internal/runtime -run '^TestP043LinuxStatusAndOSAccessRealHost$' -count=1
```

Result: `PASS`, `P043_REAL_HOST_STATUS=0`. The actual host test rejected every unsupported isolation requirement, reported `linux-host`/`ubuntu`/`os-user` with no invented service controls, and ran a shell command proving the effective `ubuntu` UID and writable selected workspace. Process status remained ready with no retained capacity after confirmed prior cleanup. This completes the P043 full real-host capability/access subset, not later R-01/R-02 or network acceptance.

## Git handoff and closeout

- Mac implementation commit: `961f0b0657882ea325ad2ecd898b8f36631b31d3`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu was clean on `dev` at `21cfa7991b27aad50705f294e7381080c9ee70ae`, then fast-forward pulled to `961f0b0657882ea325ad2ecd898b8f36631b31d3` with the specified Ubuntu key.
- Mac and Ubuntu were verified equal at `961f0b0657882ea325ad2ecd898b8f36631b31d3` before the real-host test.
- This evidence update is the separate P043 closeout change and must be pushed and fast-forwarded before P044 begins.
