# P005 — domain identities, immutable target, and import boundaries

## Pre-phase record

- **Status:** PASS. The implementation and local gates passed, and the phase was pushed and fast-forwarded to Ubuntu. This handoff record is finalized in a documentation-only P005 closeout commit.
- **Pre-phase HEAD:** `5df07abbf9b20caace6dca8090ccc26e6554e1dd` on `dev`.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, user `tomasz.walczuk` (UID 501), Darwin arm64. Go 1.27.1 is `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`; use `GOTOOLCHAIN=local` and phase-specific caches under `/private/tmp`.
- **P004 delivery gate:** Mac HEAD, GitHub `refs/heads/dev`, and Ubuntu `/home/ubuntu/projects/remote-session-runner` all report `5df07abbf9b20caace6dca8090ccc26e6554e1dd`. Mac and Ubuntu are on `dev` with clean worktrees. GitHub was queried using the required Mac key; Ubuntu was checked over the configured strict-host-checked SSH alias. The SSH client printed its known-hosts temporary-file permission warning, but the read-only SSH command succeeded and returned the expected clean checkout.

## Fresh-context read inventory

Read through EOF in this P005 context:

- `AGENTS.md` (Mac-only versioned changes, scoped commit, explicit-key GitHub push, clean Ubuntu pre-pull, fast-forward pull, same-commit gate)
- `020-initial-design/010-remote-session-runner-initial-design.md` (566 lines)
- `030-detailed-design/010-remote-session-runner-detailed-design.md` (605 lines; D-12 package map and test case reviewed)
- `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md` (267 lines; §1 serial/fresh-context gates and P005 row reviewed)
- `040-implementation-evidence/000-preimplementation-decisions.md` (74 lines)
- `040-implementation-evidence/P002.md` (tracked in-progress P002 evidence; retained unchanged)
- `040-implementation-evidence/P004.md` (prior-phase evidence)
- `README.md`, `go.mod`, current `src/internal` Go package inventory, package stubs, and `src/internal/domain/schema_test.go`
- Existing P002 domain JSON schemas and fixtures were inventoried; they remain unchanged in this phase.

The tracked baseline blobs are: `AGENTS.md` `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5`, README `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5`, initial design `12440598a0dba018313196cd80c7848284868f28`, detailed design `b24f095d62e6000abed690abf125fd60be4eff1c`, phased plan `3f7de2b5ed99332636f15b9d947be5d5662e5f53`, decisions `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec`, P002 evidence `6f51b30e071cec86d49a8ee8c853f38cdde022bc`, P004 evidence `8c6a11e9b797dc74f13e217c82ca729143bb4728`, and `go.mod` `c9dbe1d7c9e011c05e96349b21b350a6d2733c56`.

## P005 scope and gate

Add distinct opaque domain ID types for the session, command, job, mailbox request, and local intent; controller type/identity values for the selected `local_user`, `queued_mac`, and `direct_mtls` namespaces; and a value-style execution target with only `local`/`remote` kinds and a non-empty profile. This phase validates identity shape only: it does not generate IDs or add environment/profile compatibility policy, which belongs to P006.

Add a static production-Go import graph check that discovers source files and package directories under `src` as they are added, then checks the D-12 initial graph boundaries: domain cannot reach transport, store, or runtime packages; local API cannot reach SSH bridge or runtime packages; SSH bridge cannot reach the shared execution/scheduler package. Test files are excluded from the production import graph. The scanner is expected to check transitive paths as well as direct imports and to include a fixture proving that newly added packages are discovered.

Focused gate: domain P005 type and import-boundary tests. Full gates: `go test ./...`, `go vet ./...`, and diff checks. No concurrent behavior changes, so a race run is not required by this phase. P005 has no remote Runner application or host gate; the temporary preflight TLS probe remains transport-only evidence.

## Assumptions and later gates

- The full dual-target PoC remains selected: local commands run as `tomasz.walczuk`; Linux commands run directly as `ubuntu`; no Podman and no tunnels; direct HTTPS uses public `129.151.232.40:8443` with mandatory mTLS and Ubuntu bind `10.0.0.200:8443`.
- P143's software-crash-only durability path still requires explicit user approval if representative physical power-cut tests are unavailable. No approval is assumed. P005 passes no future host/runtime gate.

## Implementation and delivery record

Added typed opaque IDs for session, command, job, mailbox request, local intent, and controller; the three selected controller namespaces; and an immutable value-style execution target. The constructors reject empty IDs/profiles and unsupported controller/target kinds without changing opaque values. Environment/profile compatibility and ID generation remain for later phases. Updated the domain package comment.

Added a static production-Go import graph scan across `src`. It checks direct and transitive D-12 edges for every package under the protected domain, local API, and SSH bridge package roots. A temporary fixture adds nested packages, including a transitive domain-to-runtime edge, and confirms the scanner discovers and rejects the new forbidden dependencies. Test files are excluded from the production graph.

On the Mac with Go 1.27.1 (`GOTOOLCHAIN=local`, `GOCACHE=/private/tmp/remote-session-runner-p005-go-cache`, `GOMODCACHE=/private/tmp/remote-session-runner-p003-modcache`, `GOPATH=/private/tmp/remote-session-runner-p005-gopath`), all required local gates passed:

1. `go test ./src/internal/domain -run '^TestP005' -count=1` — PASS, exit 0.
2. `go test ./...` — PASS, exit 0, including existing P002–P004 contract tests.
3. `go vet ./...` — PASS, exit 0.
4. `gofmt -d` on the changed Go files — PASS, no output.
5. `git diff --check` and staged `git diff --cached --check` — PASS, exit 0.

No race run was required because this phase adds no concurrent behavior. P005 does not start a Runner application or exercise a host gate. `040-implementation-evidence/P002.md` remains unchanged at baseline blob `6f51b30e071cec86d49a8ee8c853f38cdde022bc`.

The scoped P005 implementation commit is `c9b72db54e0ce8f2a08c54ef75a78b99e92c62e0` (`phase(P005): add domain identities and import boundaries`). On the Mac, it was pushed with the required identity using `git -c core.sshCommand='ssh -i /Users/tomasz.walczuk/.ssh/gavrilo123456-github -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes' push origin dev`; the command exited 0. Before pulling, Ubuntu was verified on clean `dev` at P004 commit `5df07abbf9b20caace6dca8090ccc26e6554e1dd`. The fast-forward pull used the required Ubuntu key with `git -c core.sshCommand='ssh -i /home/ubuntu/.ssh/gavrilo123456-github -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=yes' pull --ff-only origin dev`; it exited 0. After the pull, Mac HEAD, GitHub `refs/heads/dev`, and Ubuntu HEAD all equaled `c9b72db54e0ce8f2a08c54ef75a78b99e92c62e0`; Mac and Ubuntu worktrees were clean. Strict host-key checking remained enabled. The SSH client's known-hosts temporary-file permission warning did not prevent the verified SSH and Git operations.

No Runner application or remote host test was run or counted as a P005 gate. The preflight TLS probe remains transport-only evidence. The documentation-only closeout containing this delivery record must also be pushed and fast-forwarded before beginning P006.
