# P038 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P038 — add local `empty`, exact `git_revision`, and explicit `local_worktree` source/provenance.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** versioned changes were made on the Mac. Ubuntu received only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `86efed13cf0231e64949e1fc9753b65db0684087` (`docs(P037): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P037 implementation `d338b0cd8bfbedcc8b695c678f6a953e539612c8`; P037 closeout `86efed13cf0231e64949e1fc9753b65db0684087`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P037.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including P-MAC-02, the P038 row, exact commit resolution, session worktree/source provenance, local uncommitted-file visibility, non-portable local worktrees, and the P143 limitation;
- current P030–P037 runtime implementation/tests and all prior gates.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P037 evidence | `6d866ca61425ae8ef3493880e601b864b955e862` |

## Planned deliverable and gates

1. Resolve `empty` into a private portable workspace.
2. Resolve an approved repository alias and requested revision to an exact commit, then clone/check out that commit into a session workspace without copying uncommitted source.
3. Resolve `local_worktree` only from an existing absolute directory, record its canonical path, retain its uncommitted files, and mark it non-portable; cleanup must never remove it.
4. Exercise all three provenance modes on the actual Mac checkout/account.

Required gates: focused P038 Mac source tests, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. No host-dependent SSH, HTTPS, mTLS, or Runner application test is assigned to P038. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Implementation results

**Status: PASS**

- Extended `MacProcessAdapter` with source modes, exact Git revision resolution, detached shared clone/check out, canonical local-worktree validation, portable/non-portable provenance, ownership flags, and safe cleanup for prepared-but-not-started sessions.
- Added `src/internal/runtime/p038_test.go`: actual Mac `empty`, exact current HEAD Git revision, and local-worktree uncommitted marker visibility all passed.
- The small deterministic P035 stop-test adjustment is included in this phase commit; it accepts the host signal race only when no false terminal/replacement claim is made.

The implementation commit was `b48c13fa759fd3ff060d4b2f1dffa875e9c631dd` (`phase(P038): add local source provenance`).

### Mac gates

```text
go test ./src/internal/runtime -run '^TestP03[012345678]' -count=1 PASS
go test ./src/internal/store ./src/internal/execution -count=1    PASS
make test                                                          PASS
make vet                                                           PASS
make smoke                                                         PASS
go test -race ./...                                                PASS
find src -name '*.go' -print0 | xargs -0 gofmt -d               PASS (no output)
git diff --check                                                     PASS
```

All commands ran on the Mac as `tomasz.walczuk` with Go 1.27.1 and hermetic caches under `/private/tmp`. The known non-fatal Go telemetry upload-token warning was present and did not affect results. No host-dependent SSH, HTTPS, mTLS, runtime, or Runner application test was assigned to P038. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Git handoff and closeout

- Mac implementation commit: `b48c13fa759fd3ff060d4b2f1dffa875e9c631dd`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu was clean on `dev` at `86efed13cf0231e64949e1fc9753b65db0684087`, then fast-forward pulled to `b48c13fa759fd3ff060d4b2f1dffa875e9c631dd` with the specified Ubuntu key.
- This evidence file is the separate P038 closeout change; it must be pushed and fast-forwarded before P039 begins.
