# P036 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P036 — add descendant inspection/cleanup and retained capacity for uncertain stop/EOF.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** versioned changes were made on the Mac. Ubuntu received only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `dbf81d2d06d4208fe5761c91a7502241d309d38b` (`docs(P035): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P035 implementation `3986f66f71dab4c172deef302ca58cf87b4396e0`; P035 closeout `dbf81d2d06d4208fe5761c91a7502241d309d38b`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P035.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including the P036 row, R-03 descendant/retained-capacity rules, no replacement shell, process-group limitations, later host-process gates, and the P143 limitation;
- current P030–P035 runtime implementation/tests, execution capacity seams, and the complete prior test gates.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P035 evidence | `b58926a7010676b30a821902b2dc238556ea276f` |

## Planned deliverable and gates

1. Add best-effort process-tree inspection from the host process table and bounded TERM/KILL descendant cleanup.
2. Expose injectable inspector/killer seams for deterministic tests while retaining the real `ps` and OS-signal implementation for host adapters.
3. Retain capacity after uncertain output/stop boundaries, release the retention only after confirmed cleanup, and refuse all later commands under the lost session.
4. Exercise a residual descendant fixture, cleanup confirmation, capacity-retention transition, and no replacement shell.

Required gates: focused P036 tests, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. Actual Mac/Linux host process identity and descendant gates remain assigned to P037–P042; P036's process-tree fixture is hermetic. No host-dependent SSH, HTTPS, mTLS, or Runner application test is assigned to P036. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Implementation results

**Status: PASS**

- Added `DescendantProcess`, `DescendantCleanupResult`, `InspectDescendants`, `CleanupDescendants`, and `CapacityRetained` to the persistent shell runtime.
- The default process inspector parses `ps -axo pid=,ppid=,command=` recursively; cleanup sends bounded TERM/KILL signals and records whether residual capacity can be released.
- Added injectable inspector/killer seams so the P036 residual test is deterministic in the restricted Mac shell while later host phases can exercise actual process identity and descendants.
- Added `src/internal/runtime/p036_test.go` covering uncertain residual retention, confirmed cleanup release, and no replacement command. The small P035 stop-test adjustment is included in the implementation commit to avoid treating a host signal race as a false terminal claim.

The implementation commit was `190eca87ebf3f356883a92c098338be42cb20691` (`phase(P036): add descendant cleanup and capacity retention`).

### Mac gates

All commands ran in `/Users/tomasz.walczuk/projects/remote-session-runner` as `tomasz.walczuk` with Go 1.27.1 and hermetic `GOCACHE`, `GOMODCACHE`, and `GOPATH` under `/private/tmp`:

```text
go test ./src/internal/runtime -run '^TestP03[0123456]' -count=1 PASS
go test ./src/internal/store ./src/internal/execution -count=1    PASS
make test                                                          PASS
make vet                                                           PASS
make smoke                                                         PASS
go test -race ./...                                                PASS
find src -name '*.go' -print0 | xargs -0 gofmt -d               PASS (no output)
git diff --check                                                     PASS
```

The selected Go binary emitted the known non-fatal telemetry warning because the Codex shell cannot write the user's Go telemetry upload-token path. It did not affect any test, vet, build, smoke, or race result.

No host-dependent SSH, HTTPS, mTLS, runtime, or Runner application test was assigned to P036. The temporary TLS probe remains transport evidence only, and P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Git handoff and closeout

- Mac implementation commit: `190eca87ebf3f356883a92c098338be42cb20691`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu `/home/ubuntu/projects/remote-session-runner`: was clean on `dev` at `dbf81d2d06d4208fe5761c91a7502241d309d38b`, then fast-forward pulled `origin/dev` with the specified Ubuntu key to `190eca87ebf3f356883a92c098338be42cb20691`.
- This evidence file is the separate P036 closeout change; it must be pushed and fast-forwarded before the next phase begins.
