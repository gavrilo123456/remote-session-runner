# P041 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P041 — track Linux agent/Bash process groups and descendants for command stop, teardown, and residual-capacity accounting.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; named real-host gate is Ubuntu `oracle-yuta-konopka-ubuntu-micro-02`, account `ubuntu`.
- **Authoritative source:** versioned changes are made on the Mac. Ubuntu receives changes only through the pushed GitHub `dev` revision and a fast-forward pull.
- **PoC boundary:** local execution remains `tomasz.walczuk`, remote execution remains `ubuntu`; no Podman, tunnels, or Runner filesystem confinement. Direct HTTPS/mTLS and the temporary TLS probe remain separate later gates. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.
- **Pre-phase Mac HEAD:** `1bac5616665a552ee68e3164d7f2efba2e49ebc3` (`docs(P040): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P040 implementation `6d1b56375be5ca9d057d1d4f2569ef7430347cbe`; P040 closeout `1bac5616665a552ee68e3164d7f2efba2e49ebc3`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P040.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including P041, P-LNX-01, process-group/descendant ownership, retained capacity after uncertain cleanup, and the P143 limitation;
- current `src/internal/runtime/linux.go`, `persistent.go`, P035/P036/P040 runtime tests, and the existing process inspector/signal seams.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P040 evidence | `07d009f87f42409f8566272fef93c300914cbb3e` |

## Planned deliverable and gates

1. Expose Linux process-group identity and descendant observations through the P040 adapter while continuing to use the shared `PersistentShell` implementation.
2. Add adapter methods for command stop, descendant cleanup, and capacity-retained state; never create a replacement shell after an uncertain stop or output boundary.
3. Exercise an actual Ubuntu process group, a known background child, bounded cleanup, and a long-running command stop on the designated host.

Required gates: focused P041 unit and real-Linux process tests, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. The Ubuntu test must run only after the Mac implementation is pushed and the Ubuntu checkout is fast-forwarded to the same commit. P041 does not claim restart reconciliation, full P-LNX-01 policy coverage, HTTPS/mTLS, SSH-bridge, or two-host Runner application acceptance.

## Implementation results

**Status: PASS**

- Extended `LinuxProcessRecord` with the process-group ID and added adapter methods for `StopCommand`, `InspectDescendants`, `CleanupDescendants`, and `CapacityRetained`.
- The adapter delegates interruption and descendant cleanup to the shared `PersistentShell`; a false stop/cleanup confirmation remains conservative and retains capacity. No replacement shell is started.
- Added `src/internal/runtime/p041_test.go` for the real Linux lifecycle: process-group ownership, a known background child, confirmed bounded cleanup, capacity release after confirmation, long-running command stop, and same-generation shell identity.

## Mac gates

```text
go test ./src/internal/runtime -run '^TestP041' -count=1       PASS
go test ./src/internal/runtime ./src/internal/store ./src/internal/execution -count=1 PASS
make test                                                       PASS
make vet                                                        PASS
make smoke                                                      PASS
go test -race ./...                                             PASS
gofmt -d                                                         PASS (no output)
git diff --check                                                 PASS
```

All Mac commands used Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go` with hermetic caches under `/private/tmp`. The known non-fatal Go telemetry upload-token warning remained present and did not affect results.

## Linux real-host gate

- Host: `oracle-yuta-konopka-ubuntu-micro-02`, Ubuntu 20.04.6 LTS.
- Account: `ubuntu`, UID `1001`.
- Bash: `/usr/bin/bash`, GNU bash 5.0.17.

After the Mac implementation was pushed and Ubuntu fast-forwarded to the same commit, Ubuntu ran:

```text
GOCACHE=/tmp/remote-session-runner-p041-go-cache \
GOMODCACHE=/tmp/remote-session-runner-p039-modcache \
GOPATH=/tmp/remote-session-runner-p041-gopath \
GOTOOLCHAIN=local \
/home/ubuntu/.local/share/remote-session-runner/toolchains/go1.27.1/bin/go \
  test ./src/internal/runtime -run '^TestP041LinuxProcessAdapterTracksGroupDescendantsAndStop$' -count=1
```

Result: `PASS`, `P041_REAL_HOST_STATUS=0`. The test observed a Bash process group with group ID equal to its PID, found a known `sleep` descendant, confirmed bounded descendant cleanup and released capacity, then stopped a long-running command and verified the adapter kept the same shell pointer rather than creating a replacement. One initial post-push equality probe hit a transient SSH timeout; the required clean revision check was retried successfully before the test ran. This is a real Linux process/child interruption subset, not full restart reconciliation or the later complete P-LNX-01 gate.

## Git handoff and closeout

- Mac implementation commit: `e926fefead09c17ce37800a90573d6997c5c5b9e`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu was clean on `dev` at `1bac5616665a552ee68e3164d7f2efba2e49ebc3`, then fast-forward pulled to `e926fefead09c17ce37800a90573d6997c5c5b9e` with the specified Ubuntu key.
- Mac and Ubuntu were verified equal at `e926fefead09c17ce37800a90573d6997c5c5b9e` before the real-host test.
- This evidence update is the separate P041 closeout change and must be pushed and fast-forwarded before P042 begins.
