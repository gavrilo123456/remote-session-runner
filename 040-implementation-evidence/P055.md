# P055 implementation evidence

## Pre-phase scope and authority

- **Phase:** P055 — package Linux SSH forced-command restrictions and test server-side escape/controller negatives.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, source account `tomasz.walczuk`; named host gate is Ubuntu `oracle-yuta-konopka-ubuntu-micro-02`, account `ubuntu`.
- **Authoritative source:** tracked changes are made only in the Mac checkout. Ubuntu receives the committed revision through GitHub `dev` and a fast-forward pull.
- **PoC boundary:** Mac commands remain `tomasz.walczuk`; Linux commands remain `ubuntu`; no Podman, tunnel, or Runner filesystem confinement. Public HTTPS/mTLS and the temporary TLS probe remain separate later gates. P143 software-crash-only durability still requires explicit user approval if physical power-cut tests are unavailable.
- **Pre-phase Mac HEAD:** `1ce9a235df91ff84b141e494508ab3df79d7d07f` (`docs(P054): record bridge event evidence`), clean on `dev...origin/dev` before P055 edits.
- **Prior phase:** P054 implementation plus evidence closeout, synchronized cleanly to Ubuntu.

## Fresh-context read inventory

Read through EOF before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P054.md`;
- complete current initial design, detailed design, and phased plan, including P055, forced-command restrictions, pinned host-key/controller boundaries, SSH escape negatives, D-12/D-14, P-NET-01, and serial delivery rules;
- current `runner-ssh-bridge` entrypoint, bridge protocol/server/forwarder/schema/tests, config package and fixtures, private runnerd socket/server, and all prior bridge evidence;
- actual public dispatcher key identity (fingerprint `SHA256:FnYdf11ON1ZHh+eCnkZbHvjePKktcH7Z/rDjv3nWsms`), pinned host key, current Ubuntu account/hostname, and current `~ubuntu/.ssh/authorized_keys` mode/state. The dedicated dispatcher key is not yet authorized on Ubuntu.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P054 evidence | `7da6579f49a04d161f67d257bb95e021152eea19` |

## Planned deliverable and gates

1. Add a small deployment package that renders and validates an owner-controlled `authorized_keys` entry with `restrict` and one constant forced command, rejecting shell interpolation, option widening, malformed keys, non-absolute paths, and unsafe permissions.
2. Add versioned SSH deployment notes/templates and a forced-command wrapper that accepts only the bridge's fixed `--stdio` invocation, with no PTY, agent/X11, or TCP forwarding path and no controller identity from request JSON.
3. Add hermetic negative fixtures for requested-command override, PTY/forward option widening, malformed/alternate entries, and server-side controller mapping; the bridge frame controller remains derived from the authenticated key map.
4. Install a temporary P055 forced-command fixture on the named Ubuntu host outside the Git checkout, using the dedicated public dispatcher key and the verified host pin. Run real SSH negatives for arbitrary command/PTY/forward attempts and a positive fixed command, then remove the temporary host fixture. This is the P-NET-01 server subset; it is not the later full Mac SSH client gate.
5. Run focused P055 tests, full Mac hermetic/vet/build/smoke/race/format/diff gates, synchronize and verify Mac/GitHub/Ubuntu equality, then run the named Ubuntu/SSH host gate. Close evidence only after the host gate passes.

## Unresolved assumptions

- The real P055 host fixture will use a temporary owner-controlled authorized key entry and wrapper outside Git, while tracked templates document the exact restrictions and invocation. Existing Ubuntu development-login authorization remains untouched and is not evidence for the dedicated dispatcher identity.
- The wrapper checks `SSH_ORIGINAL_COMMAND` against one fixed bridge command and does not parse or execute arbitrary requested shell text. OpenSSH `restrict` supplies the server-side no-PTY/no-forward/no-agent/no-X11 restrictions; the wrapper fails closed if a prohibited request still reaches it.
- P055 validates the server-side forced-command boundary and authenticated controller handoff only. Full bridge transport/pinned Mac SSH client and real two-host reconnect remain P056/P123; P143 durability approval remains unresolved.

## Implementation and hermetic gates

- **Implementation:** PASS. Added `src/internal/sshdeploy/authorized_keys.go` and tests for exact `restrict,command="<absolute bridge> --stdio"` rendering, raw `ssh-ed25519` validation, exact-command validation, and rejection of malformed keys, option widening, shell metacharacters, unsafe paths, and mismatched original commands.
- **Deployment fixture:** PASS. Added executable `deploy/ssh/runner-ssh-bridge-forced.sh` and `deploy/ssh/README.md`. The wrapper compares `SSH_ORIGINAL_COMMAND` byte-for-byte with `runner-ssh-bridge --stdio`, rejects `SSH_TTY`, and never evaluates the request as shell text. OpenSSH `restrict` supplies the no-PTY/no-agent/no-X11/no-TCP-forwarding restrictions.
- **Mac focused test:** `go test ./src/internal/sshdeploy -run '^TestP055' -count=1 -v` — PASS.
- **Mac repository gates:** `make test` — PASS; `make vet` — PASS; `make build` — PASS; `make smoke` — PASS; `go test -race ./...` — PASS; `git diff --cached --check` — PASS.
- **Implementation commit:** `b7a149333675267f9e57adc7ae31832c3a240f97` (`phase(P055): package SSH forced-command restrictions`). The commit was pushed to GitHub `origin/dev` with the dedicated Mac GitHub key and fast-forward pulled on Ubuntu. Mac, `origin/dev`, and Ubuntu were equal at this commit before the host gate.

## Named Ubuntu host gate

- **Host/account:** Ubuntu `129.151.232.40` as `ubuntu`; the SSH host key was checked against the pinned file `~/Library/Application Support/RemoteSessionRunner/secrets/ssh_known_hosts`, fingerprint `SHA256:ezxpJBu2U3XNiWVkm+G4FX4Hx7lyQpnOAaidHZl4CQM`.
- **Dedicated identity check:** the dispatcher key was tested with `-F /dev/null -l ubuntu -i .../dispatcher_ed25519 -o IdentitiesOnly=yes`, so the Mac user SSH configuration and its development key could not be used as a fallback. Before the fixture, authentication failed with `Permission denied (publickey)` (rc 255).
- **Temporary fixture:** outside the Git checkout at `/home/ubuntu/.local/share/remote-session-runner/p055-host-fixture`. The original `/home/ubuntu/.ssh/authorized_keys` was backed up, the temporary wrapper and one `restrict,command=".../forced-command.sh" ssh-ed25519 ... runner-mac-dispatcher-p055` entry were installed with owner-only modes, and the backup was restored after testing.
- **Fixed command:** `runner-ssh-bridge --stdio` — PASS, rc 0, output `P055_FORCED_OK` using the dedicated dispatcher key and pinned host key.
- **Alternate command:** requested `sh -c id` — PASS as a negative, rc 126, wrapper output `p055 forced command denied`.
- **PTY:** `ssh -tt ... 'runner-ssh-bridge --stdio'` — PASS as a negative, rc 255, OpenSSH output `PTY allocation request failed on channel 0`.
- **TCP forwarding:** a temporary wrapper kept the forced-command session alive; `-L 127.0.0.1:34567:127.0.0.1:22` was exercised and a local channel attempt produced `channel 2: open failed: administratively prohibited: open failed`. The local listener itself accepted the TCP connect before the server rejected the forwarded channel; the SSH forwarding channel was denied as required.
- **Restoration:** the original `authorized_keys` was restored and the fixture directory removed. Final checks showed mode `600`, owner `ubuntu:ubuntu`, no fixture directory, and the dedicated dispatcher key again failed with `Permission denied (publickey)`.
- **Controller mapping:** the authenticated controller-to-key mapping and forged-controller rejection remain covered by the hermetic P049/P054 tests. The Ubuntu host fixture is the forced-command/PTY/forward server subset only; it is not the P056 Mac SSH client, public HTTPS/mTLS, the temporary TLS probe, or a Runner application event test.

## Phase result

**P055 PASS.** Required Mac, GitHub, Ubuntu synchronization, and named host restrictions gates passed. Proceed to P056 only after a fresh-context read of the current documents and this evidence. P143 software-crash-only durability remains an explicit approval gate if physical power-cut tests are unavailable.
