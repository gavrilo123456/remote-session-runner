# P029 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P029 — integrate fake create/cleanup/restart/GC races and residual command slots.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** versioned changes were made on the Mac. Ubuntu received only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `680f234bba60ee69c8a58fd010aeff1077bf3e84` (`docs(P028): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P028 implementation `ab9c3d2195969da8c1c81bbfb736a13cc96e9899`; P028 closeout `680f234bba60ee69c8a58fd010aeff1077bf3e84`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P028.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including the P029 row, D-15, D-17, startup/restart recovery, retention pinning, race requirements, and the P143 limitation;
- current fake execution service/runtime, authority migrations/store, scheduler, reconciliation/GC implementation, P019/P027/P028 tests, and the complete prior test gates.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P028 evidence | `cf5a454a071aa15e4691863e2e45fa42721b53b5` |

## Planned deliverable and gates

1. Race more than 20 fake session creates against the transactional reservation ceiling and verify exactly 20 live reservations.
2. Close the successful sessions, advance the fake clock beyond 90 days, and verify terminal authority metadata/idempotency cleanup.
3. Restart a fake executor with a running command, preserve its residual slot through 90-day GC, and prove that confirmed command/session cleanup releases it exactly once.
4. Run the integrated tests under the race detector and preserve the no-shell-reattachment and no-capacity-reuse rules from P027/P028.

Required gates: focused P029 integration tests, store/execution tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. No host-dependent runtime, SSH, HTTPS, mTLS, or Runner application test is assigned to P029. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Implementation results

**Status: PASS**

- Added a thread-safe fake runtime/publisher integration fixture.
- Added a concurrent 24-create test proving the host reservation ceiling admits exactly 20 sessions, rejects the excess, and later GCs all confirmed terminal metadata after the 90-day boundary.
- Added an integrated restart/GC test proving a running command becomes lost without reattachment, its command slot and session reservation remain live beyond 90 days, and confirmed cleanup permits one subsequent metadata deletion pass.
- The P029 test suite itself passes under `-race`; the full repository race suite also passes.

The implementation commit was `3aa171e0a7c9827e4fdba46036cf4a1aa9c49308` (`phase(P029): integrate restart retention and capacity races`).

### Mac gates

All commands ran in `/Users/tomasz.walczuk/projects/remote-session-runner` as `tomasz.walczuk` with Go 1.27.1 and hermetic `GOCACHE`, `GOMODCACHE`, and `GOPATH` under `/private/tmp`:

```text
go test ./src/internal/execution -run '^TestP029' -count=1       PASS
go test -race ./src/internal/execution -run '^TestP029' -count=1 PASS
go test ./src/internal/store ./src/internal/execution -count=1   PASS
make test                                                       PASS
make vet                                                        PASS
make smoke                                                      PASS
go test -race ./...                                             PASS
find src -name '*.go' -print0 | xargs -0 gofmt -l                PASS (no output)
git diff --check                                                PASS
```

The selected Go binary emitted the known non-fatal telemetry warning because the Codex shell cannot write the user's Go telemetry upload-token path. It did not affect any test, vet, build, or smoke result.

No host-dependent SSH, HTTPS, mTLS, runtime, or Runner application test was assigned to P029. The temporary TLS probe remains transport evidence only, and P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.

## Git handoff and closeout

- Mac implementation commit: `3aa171e0a7c9827e4fdba46036cf4a1aa9c49308`.
- GitHub `origin/dev`: verified with the specified Mac key at the same commit.
- Ubuntu `/home/ubuntu/projects/remote-session-runner`: was clean on `dev` at `680f234bba60ee69c8a58fd010aeff1077bf3e84`, then fast-forward pulled `origin/dev` with the specified Ubuntu key to `3aa171e0a7c9827e4fdba46036cf4a1aa9c49308`.
- Final implementation handoff verification: Mac, GitHub `dev`, and Ubuntu all reported `3aa171e0a7c9827e4fdba46036cf4a1aa9c49308`; Ubuntu remained clean. This evidence file is the separate P029 closeout change.
