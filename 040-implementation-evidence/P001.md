# P001 — Go project skeleton

## Pre-phase record

- **Status:** PASS; the phase-scoped commit is complete.
- **Pre-phase HEAD:** `81c7a99fe6bd36cd901266e620869f4c120c4d6f` (`dev`; clean worktree; 33 commits ahead of `origin/dev`).
- **Prerequisite decision commit:** `81c7a99fe6bd36cd901266e620869f4c120c4d6f`; it adds only `040-implementation-evidence/000-preimplementation-decisions.md`.
- **Design/plan blobs at pre-phase HEAD:**
  - Initial design: `12440598a0dba018313196cd80c7848284868f28`
  - Detailed design: `b24f095d62e6000abed690abf125fd60be4eff1c`
  - Phased plan: `3f7de2b5ed99332636f15b9d947be5d5662e5f53`
  - Decision note: `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec`
  - README: `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5`
- **Execution machine:** this Mac, repository `/Users/tomasz.walczuk/projects/remote-session-runner`, user `tomasz.walczuk` (UID 501), Darwin arm64. P001 Go commands and smoke checks run here as this user.
- **Selected Go toolchain:** Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`; the executable reports `go version go1.27.1 darwin/arm64`. It is not on the default `PATH`; the Makefile will select this pinned executable on macOS and allow an explicit `GO` override.

## Read inventory

Freshly read through EOF before implementation:

- `020-initial-design/010-remote-session-runner-initial-design.md` (566 lines)
- `030-detailed-design/010-remote-session-runner-detailed-design.md` (605 lines)
- `030-detailed-phased-implementaion-plan/010-remote-session-runner-detailed-phased-implementaion-plan.md` (267 lines)
- `040-implementation-evidence/000-preimplementation-decisions.md` (74 lines)
- `README.md` (2 lines)

Also checked applicable repository instructions: no `AGENTS.md` is tracked or found by `rg --files`. Inspected the full P001 prerequisite commit/diff; the decision note is its only added file. Inspected the complete tracked tree at pre-phase HEAD: it contains design/evidence Markdown and the legacy reference document/assets, with no Go module/source, tests, schemas, migrations, configuration, or deployment files relevant to P001. `git status --porcelain=v2` was empty.

The pre-implementation note records the selected dual-target design: Mac services/commands as `tomasz.walczuk`, Linux services/commands directly as `ubuntu`, no Podman and no tunnels, public direct HTTPS `129.151.232.40:8443` bound to `10.0.0.200:8443` with mandatory mTLS, and selected environment/source/controller policies. Those host checks are prerequisites only; this phase does not claim any later host gate passed. Physical power-cut testing and the conditional P143 software-crash-only approval remain outside P001.

## Deliverable and gate

P001 will add a root `go.mod`, pinned Go version/toolchain declaration, a root Makefile, Go package skeleton under `src/`, six help/version-only command stubs under `src/cmd/`, and a hermetic temporary-fixture helper. It adds no Runner execution behavior or external dependencies.

Planned checks from the repository root on the Mac:

1. `GOTOOLCHAIN=local "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" test ./...`
2. `GOTOOLCHAIN=local "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" vet ./...`
3. `make smoke` using the pinned macOS toolchain; build each command stub and invoke its help and version flags.
4. `git diff --check`, inspect the complete phase diff, record exact results, create one non-empty phase-scoped commit, and verify a clean post-commit worktree.

## Assumptions and unresolved items

- The module path is `remote-session-runner`, matching the repository name; no canonical VCS module path is specified in the current design or README.
- The six command names follow the detailed-design executable list: `runner`, `runner-local`, `runner-locald`, `runnerd`, `runner-ssh-bridge`, and `runner-session-agent`.
- No P001 prerequisite is unresolved. The dedicated SSH forced-command authorization, Runner mTLS principal mapping/listener, real runtime gates, and P143 durability decision remain later work and are not treated as passed here.

## Implementation and verification

Implemented the root Go module/toolchain pin and `.go-version`, a Makefile that selects the recorded Go 1.27.1 binary on Mac/Linux (with `GO` override), package documentation stubs for the detailed-design internal packages, six help/version-only command entrypoints, and `internal/testfixture.Root` for private files beneath a test-owned temporary directory. Added focused tests for the stub help/version behavior and fixture containment/mode. No runtime, API, persistence, network, or host-process behavior was added; no third-party modules are required.

Execution machine for all results below: this Mac (`tomasz.walczuk`, Darwin arm64), repository root. Go reports `go1.27.1 darwin/arm64`. The sandbox denied Go's default `~/Library/Caches/go-build` path (`operation not permitted`) on the first direct test/vet/smoke attempts. Rerunning with `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache` resolved that environment-only issue.

| Command | Result |
| --- | --- |
| Pinned `gofmt -w` on all Go source files | Exit 0 |
| `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache GOTOOLCHAIN=local "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" test ./...` | Exit 0; all packages compiled and both focused test packages passed. |
| `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache GOTOOLCHAIN=local "/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go" vet ./...` | Exit 0. |
| `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache make smoke` | Exit 0; built each of the six command stubs and invoked `--help` and `--version` on each. |
| `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache make build` | Exit 0; all six command packages build. |
| `GOCACHE=/private/tmp/remote-session-runner-p001-go-cache make check` | Exit 0; Makefile `test`, `vet`, and `smoke` targets all passed. |

`git diff --cached --check` exited 0; the complete staged diff was inspected. P001 has no real-process or remote-host execution gate; its focused gate is the Mac Go/package and executable smoke above. No Mac/Linux runtime, SSH bridge, HTTPS application, two-host, performance, or durability gate is claimed as passed. The post-commit worktree is clean; record this phase commit's hash in the P002 preflight, as required by §1.2.
