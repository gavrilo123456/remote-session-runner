# P020 implementation evidence

**Status: PASS**

## Pre-phase scope and authority

- **Phase:** P020 — fake-runtime create/ready/failure orchestration through the shared `ExecutionService`.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; selected Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go`.
- **Authoritative source:** all versioned changes are made on the Mac. Ubuntu receives only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman or tunnels; direct HTTPS/mTLS and actual-host Runner gates remain later phases. The temporary TLS probe is not a Runner application test.
- **Pre-phase Mac HEAD:** `d4143d887aceb9b93123d066ba5ae3f4cc2e4464` (`docs(P019): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P019 implementation `bdf9ff512113c1ab6b06a0e49d8556ba3ac6f4d6`; P019 closeout `d4143d887aceb9b93123d066ba5ae3f4cc2e4464`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P019.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including detailed §§5–7, 14.1 (`D-02`, `D-11`), 14.2 (`I-01`), 15–17, the P020 row, and the P143 limitation;
- current domain policy/limits/identity/state types and tests;
- current authority store session acceptance, lifecycle transitions, command/scheduler code, SQLite migrations, package docs, and P011–P019 tests.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P019 evidence | `ef3bdae4c0170ba874b45fb70b462574d1a806ea` |

## Planned deliverable and gates

1. Add the shared `ExecutionService` create/read boundary with `EnvironmentResolver`, `SessionRuntime`, clock, and lifecycle publisher seams.
2. Validate environment/target/source/controller/isolation/limit policy before `AcceptSessionCreate`, so invalid requests create no authoritative session or reservation.
3. Orchestrate fake `Prepare` → `StartAgent` → `ready`; on runtime failure clean up and commit `failed`, or `lost` when cleanup cannot be confirmed. Duplicate same-key create returns the original state and never starts a second fake runtime.

Required gates: focused `D-02`/`D-11`/`I-01` fake-runtime tests, focused P011–P020 tests, full `make test`, `make vet`, selected-toolchain gofmt, `git diff --check`, import-boundary coverage, and the Mac commit → GitHub push → clean Ubuntu fast-forward pull → equal-hash verification. No host-dependent runtime, SSH, HTTPS, or Runner application TLS test is assigned to P020. P143’s software-crash-only durability path still requires explicit user approval if physical power-cut tests are unavailable.

## Implementation and test results

- **Implementation commit (Mac):** `f324675376edea2113e62dd15df5d005c447584e` (`phase(P020): add fake runtime execution orchestration`).
- Added the shared `execution.Service`/`ExecutionService` create/read boundary, resolver/clock/runtime/publisher seams, and the atomic store completion method that records runtime generation/source revision with the creating → ready/failed/lost lifecycle transition.
- **Focused P011–P020 gate (Mac, exit 0):** selected Go 1.27.1 `go test ./src/internal/execution ./src/internal/store -run '^TestP020|^TestP019|^TestP018|^TestP017|^TestP016|^TestP015|^TestP014|^TestP013|^TestP012|^TestP011' -count=1` — PASS.
- **P020 race gate (Mac, exit 0):** selected Go 1.27.1 `go test -race ./src/internal/execution ./src/internal/store -run '^TestP020' -count=1` — PASS.
- **Full execution/store race suites (Mac, exit 0):** selected Go 1.27.1 `go test -race ./src/internal/execution ./src/internal/store -count=1` — PASS.
- **Full hermetic suite (Mac, exit 0):** `GOTOOLCHAIN=local make test` (`go test ./...`) — PASS.
- **Full race suite (Mac, exit 0):** selected Go 1.27.1 `go test -race ./... -count=1` — PASS.
- **Vet gate (Mac, exit 0):** `GOTOOLCHAIN=local make vet` (`go vet ./...`) — PASS.
- **Formatting/integrity (Mac, exit 0):** selected-toolchain `go fmt ./src/internal/execution ./src/internal/store`, followed by `git diff --check` — PASS.
- **Focused assertions:** policy/target and unsupported-isolation requests leave zero authoritative reservations and never call the runtime; a committed `creating` session is observed before fake runtime calls; successful fake prepare/start records a generation and `ready`; prepare failure records `failed` only after confirmed cleanup; start failure with uncertain cleanup records `lost`; same-key retry returns the original snapshot without a second runtime start; controller mismatch is denied on read.
- The known Go 1.27.1 telemetry upload-token permission warning appeared in the Codex shell; all commands exited 0. No actual Mac/Linux process, SSH, public HTTPS, or Runner application TLS gate is claimed by P020.

## Git handoff and closeout

- **Mac pre-push:** implementation commit `f324675376edea2113e62dd15df5d005c447584e` on `dev`; only this evidence file remained untracked.
- **GitHub:** `origin/dev` verified at `f324675376edea2113e62dd15df5d005c447584e` using `/Users/tomasz.walczuk/.ssh/gavrilo123456-github`.
- **Ubuntu pre-pull:** `/home/ubuntu/projects/remote-session-runner` was clean on `dev` at `d4143d887aceb9b93123d066ba5ae3f4cc2e4464`.
- **Ubuntu pull:** fast-forward `d4143d887aceb9b93123d066ba5ae3f4cc2e4464` → `f324675376edea2113e62dd15df5d005c447584e` using `/home/ubuntu/.ssh/gavrilo123456-github`; Ubuntu `HEAD` and `origin/dev` were verified at the implementation hash and the worktree was clean.
- **Host-dependent gates:** NOT RUN; none are required for P020. The temporary TLS probe remains distinct from a Runner application test.
- **Next phase:** P021, only after this evidence closeout is committed, pushed, pulled, and both checkouts are verified equal and clean.
