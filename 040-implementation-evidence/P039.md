# P039 implementation evidence

**Status:** PASS

## Pre-phase scope and authority

- **Phase:** P039 — add the Linux `linux-host` process-profile preflight/doctor for the designated `ubuntu` host.
- **Execution machine:** Mac checkout `/Users/tomasz.walczuk/projects/remote-session-runner`, account `tomasz.walczuk`; the named real-host gate is Ubuntu `oracle-yuta-konopka-ubuntu-micro-02`, account `ubuntu`.
- **Authoritative source:** versioned changes are made on the Mac. Ubuntu will receive only the pushed GitHub `dev` revision through a fast-forward pull.
- **PoC boundary:** local execution is `tomasz.walczuk`, remote execution is `ubuntu`; no Podman, tunnels, or Runner filesystem confinement. Direct HTTPS/mTLS and the temporary TLS probe remain separate later gates. P143's software-crash-only durability path still requires explicit approval if physical power-cut tests are unavailable.
- **Pre-phase Mac HEAD:** `1c2de47d1445c8fa9e09d072ebe0f546d97ae79e` (`docs(P038): record phase delivery evidence`), clean on `dev...origin/dev`.
- **Prior phase:** P038 implementation `b48c13fa759fd3ff060d4b2f1dffa875e9c631dd`; P038 closeout `1c2de47d1445c8fa9e09d072ebe0f546d97ae79e`.

## Fresh-context read inventory

Read through EOF from the current checkout before coding:

- root `AGENTS.md`, `README.md`, `040-implementation-evidence/000-preimplementation-decisions.md`, and `040-implementation-evidence/P038.md`;
- complete current initial design (566 lines), detailed design (605 lines), and phased plan (267 lines), including the Linux `linux-host` profile, P-LNX-01, P039, truthful capabilities, owner-only service paths, and the P143 limitation;
- current runtime adapter, persistent-shell/process seams, configuration path definitions, domain capability/policy types, and P005 import-boundary test.

Design blob revisions at the pre-phase HEAD:

| Artifact | Git blob |
| --- | --- |
| `AGENTS.md` | `f0ce2c194b7a5b5621994cad9ed3ca747dbce2f5` |
| `README.md` | `75cb797f1b86cfc6ab47313793dd1fdf9ae76ce5` |
| initial design | `12440598a0dba018313196cd80c7848284868f28` |
| detailed design | `b24f095d62e6000abed690abf125fd60be4eff1c` |
| phased plan | `3f7de2b5ed99332636f15b9d947be5d5662e5f53` |
| preimplementation decisions | `7c9a9dde397e8b0fee0cd0b21109dfafbd46ccec` |
| P038 evidence | `a4ff389a5eb8f50dcb93d390ed762364b6c1ee3c` |

## Planned deliverable and gates

1. Add an injectable Linux host profile doctor with explicit `ubuntu` account and `linux-host` identity.
2. Check Bash resolution/version, existing owner-only service/workspace directories, and a writable owner-only probe without following unsafe symlinks.
3. Exercise real process inspection and signalling against a short-lived Bash child, record the observed PID/UID/command, and report failures conservatively.
4. Report only the `os-user` capability and the fixed unsupported per-session controls; do not inspect or claim Podman, cgroup-v2, or sandbox isolation.

Required gates: focused P039 unit and real-Linux tests, store/execution regression tests, full `make test`, `make vet`, `make smoke`, selected-toolchain formatting and `git diff --check`, and the full race suite. The Ubuntu test must run only after the Mac implementation is pushed and the Ubuntu checkout is fast-forwarded to the same commit. No public HTTPS/mTLS, SSH bridge, two-host Runner application, or physical power-loss test is assigned to P039.

## Implementation results

**Status: PASS**

- Added `src/internal/runtime/linux.go` with the named `linux-host` profile, explicit `ubuntu` account check, owner-only path checks, Bash resolution/version check, real process-table inspection, and bounded TERM/wait probe.
- The doctor reports host/account/UID, Bash, service/workspace path modes and ownership, process identity, and readiness without exposing credentials or command output. It reports only the `os-user` boundary; service ceilings remain empty until the environment registry supplies verified Runner limits. Unsupported filesystem, CPU, memory, PID, disk, network, mount, volume, and privilege controls are explicit and never advertised as enforced.
- Added `src/internal/runtime/p039_test.go` with truthful-capability assertions, unsafe-path rejection, Mac platform behavior, and the designated Linux real-host gate. The real-host fixture explicitly chmods its service root to `0700` because Ubuntu's `umask 0002` makes `t.TempDir()` group-writable by default.

## Mac gates

```text
go test ./src/internal/runtime -run '^TestP039' -count=1             PASS
go test ./src/internal/runtime ./src/internal/store ./src/internal/execution -count=1 PASS
GOOS=linux GOARCH=amd64 go test -c ./src/internal/runtime          PASS (cross-compile only)
make test                                                           PASS
make vet                                                            PASS
make smoke                                                          PASS
go test -race ./...                                                 PASS
gofmt -d                                                             PASS (no output)
git diff --check                                                     PASS
```

All Mac commands used Go 1.27.1 at `/Users/tomasz.walczuk/Library/Application Support/RemoteSessionRunner/toolchains/go1.27.1/bin/go` with hermetic caches under `/private/tmp`. The known non-fatal Go telemetry upload-token warning remained present and did not affect results. One initial Ubuntu run correctly failed because the test fixture inherited mode `0775`; the fixture was corrected to `0700`, pushed, synchronized, and rerun. The failed run is retained as evidence that the owner-only gate is active, not waived.

## Linux real-host gate

Machine and transport:

- Host: `oracle-yuta-konopka-ubuntu-micro-02`.
- OS: Ubuntu 20.04.6 LTS.
- Account: `ubuntu`, UID `1001`.
- Bash: `/usr/bin/bash`, GNU bash 5.0.17.
- `ps`: `/usr/bin/ps`; signaling used the host process API. Podman and cgroup-v2 were not inspected by the implementation and are not profile capabilities.

At the synchronized commit, Ubuntu ran:

```text
GOCACHE=/tmp/remote-session-runner-p039-go-cache \
GOMODCACHE=/tmp/remote-session-runner-p039-modcache \
GOPATH=/tmp/remote-session-runner-p039-gopath \
GOTOOLCHAIN=local \
/home/ubuntu/.local/share/remote-session-runner/toolchains/go1.27.1/bin/go \
  test ./src/internal/runtime -run '^TestP039LinuxDoctorRealHost$' -count=1
```

Result: `PASS`, `P039_REAL_HOST_STATUS=0`. The test asserted the current UID and probe-child UID were `1001`, both service paths were directories owned by UID `1001`, mode `0700`, and writable, Bash version resolution succeeded, and a Bash child was inspected, signaled with `SIGTERM`, and reaped. This is a real Linux profile/process preflight test; it is not a Linux Runner session, HTTPS/mTLS, SSH-bridge, or two-host application acceptance test.

## Git handoff and closeout

- Mac implementation commit: `2254765dd5d7e373d5bfaa7ba1402dc196fc8064`.
- Mac fixture-correction commit after the first real-host run: `2ea6ac16471a95d1ec24b83bb44fc7f087cc3bc4`.
- GitHub `origin/dev`: verified with the specified Mac key at `2ea6ac16471a95d1ec24b83bb44fc7f087cc3bc4`.
- Ubuntu was clean on `dev` at `2254765dd5d7e373d5bfaa7ba1402dc196fc8064`, then fast-forward pulled to `2ea6ac16471a95d1ec24b83bb44fc7f087cc3bc4` with the specified Ubuntu key.
- Mac and Ubuntu were verified equal at `2ea6ac16471a95d1ec24b83bb44fc7f087cc3bc4` before the real-host test.
- This evidence update is the separate P039 closeout change and must be pushed and fast-forwarded before P040 begins.
